@model IPagedList<Post>
@{
	ViewData["PageTitle"] = "Archives";

	var postQuery = (ViewBag.PostQuery as PostQuery) ?? new PostQuery();
}

<div class="p-4">
	@if (postQuery.MonthPost > 0 && postQuery.YearPost > 0)
	{
		<h3 class="text-primary mb-3">
			Những bài viết Trong tháng '@postQuery.MonthPost' năm '@postQuery.YearPost'
		</h3>
	}

	@foreach (var postItem in Model)
	{
		<partial name="_PostList" model="postItem" />
	}
	<partial name="_Pager" model="@Model" />
</div>